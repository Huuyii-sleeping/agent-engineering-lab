## Why

左侧底部已有个人设置入口，但目前只是几个 icon 按钮，没有承载实际设置内容。需要把个人设置做成独立页面，让用户可以集中查看个人资料、偏好和本地服务状态。

## What Changes

In Scope:
- 左侧底部设置入口可切换到独立设置页。
- 设置页包含个人资料、界面偏好、运行状态三个区域。
- 设置页提供返回聊天入口。
- 页面保持当前 Web 本地状态驱动，不新增后端接口。

Out of Scope:
- 不接入登录、账号系统或远端用户资料。
- 不新增持久化后端设置。
- 不改变现有聊天发送和 SSE 协议。
