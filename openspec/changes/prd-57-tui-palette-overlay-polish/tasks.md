## 1. PRD 与规格

- [x] 1.1 新增 `PRD-57` 增量文档、proposal / design / delta spec，并在实现后同步主规格

## 2. TUI palette overlay polish

- [x] 2.1 收紧 `Command Bar` 与 `Palette Results` 的 overlay 宽度和信息层级
- [x] 2.2 调整 palette 结果行为命令优先结构，并收口打开态 keys hint

## 3. 验证

- [x] 3.1 focused tests 覆盖新的 compact overlay 文案与布局
- [x] 3.2 运行 build、OpenSpec strict 和差异检查
