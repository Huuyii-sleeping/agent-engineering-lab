## Why

历史对话和顶部标题显示 `会话 + hash` 不符合常见生产级 Chat 产品体验。成熟产品通常在首轮对话后生成会话标题，标题可以来自模型摘要，也可以先使用首条用户消息的本地摘要作为轻量实现。

## What Changes

In Scope:
- 历史列表和顶部标题优先展示用户重命名。
- 未重命名时，使用首条用户消息生成本地摘要标题。
- 空会话展示 `新对话`，不再展示 hash。

Out of Scope:
- 不新增后端标题生成接口。
- 不调用模型生成标题。
- 不改变 session 持久化格式。
