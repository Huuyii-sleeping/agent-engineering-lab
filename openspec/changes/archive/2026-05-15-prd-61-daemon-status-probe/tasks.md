## 1. PRD 与规格

- [x] 1.1 新增 `PRD-61` 增量文档、proposal / design / delta spec

## 2. daemon status 探测

- [x] 2.1 为 daemon lock 增加只读状态探测 API
- [x] 2.2 新增 `agent-cli daemon status` CLI 子命令和退出码语义

## 3. 验证与文档

- [x] 3.1 补 focused tests，并同步 README
- [x] 3.2 运行 build、OpenSpec strict 和差异检查
