## 1. PRD 与规格

- [x] 1.1 新增 `PRD-63` 增量文档、proposal / design / delta spec

## 2. host-owned 事件流

- [x] 2.1 为 `AgentHost` 增加共享事件流类型、订阅和分发能力
- [x] 2.2 让 `AgentService` 复用 host-owned event stream，并去掉 service-local 事件总线

## 3. 验证与文档

- [x] 3.1 补 focused tests，并同步 README
- [x] 3.2 运行 build、OpenSpec strict 和差异检查
