# 当前对话交接

这份文档是给换电脑继续开发时用的临时交接记录，后续可直接删除。

## 这轮已经完成什么

- 已把仓库目标从“功能型 MVP”切到“可持续迭代的真实工具”。
- 已建立 `PRD-21` 与对应 OpenSpec 变更，作为后续生产级重构主线。
- 已把 Claude Code 参考优先级固定下来：
  1. 外部 `src/` 实际源码
  2. 外部架构解析文章
  3. 当前仓库实现
  4. 本地 `typescript/` 教学代码
- 已完成第一轮核心架构收口：
  - 共享 bootstrap / composition root
  - 共享 query runtime
  - 共享 tool runtime
- 已补齐这一轮对应的中文学习沉淀文档，并删除旧的英文模板文档。
- 你的 10 条通用编码思想钢印已经转成中文并落到 [AGENT.md](/Users/bytedance/Personal/agent-engineering-lab-web/AGENT.md:43)。

## 当前代码状态

本轮重构的核心目标，是让 CLI、HTTP service、未来 Web 接入不要再各走各的装配与执行路径，而是逐步收敛到同一条主线。

已经落地的代码边界：

- `apps/agent-cli/src/bootstrap/app-runtime.ts`
  - 抽出共享应用装配入口。
  - `cli.ts`、`server.ts`、`agent-service.ts` 都开始复用这套装配。
- `apps/agent-cli/src/runtime/query-runtime.ts`
  - 抽出统一 query 执行入口。
  - 用户消息注入、系统消息拼接、工具解析、循环执行、结果提取开始走共享路径。
- `apps/agent-cli/src/runtime/tool-runtime.ts`
  - 抽出统一 tool 执行边界。
  - 开始显式区分 `base / subagent / mcp` 执行目标，并收口保护执行逻辑。
- `apps/agent-cli/src/tools/index.ts`
  - 已改成先解析执行目标，再进入统一执行路径。

## 已补的测试

- `apps/agent-cli/test/unit/bootstrap/app-runtime.test.ts`
- `apps/agent-cli/test/unit/runtime/query-runtime.test.ts`
- `apps/agent-cli/test/unit/runtime/tool-runtime.test.ts`
- `apps/agent-cli/test/unit/agent-service.test.ts`
- `apps/agent-cli/test/unit/cli.test.ts`
- `apps/agent-cli/test/smoke/prd12-service-api-smoke.ts`

## 已验证状态

之前这轮开发里已经通过过这些验证：

- `pnpm --filter agent-cli build`
- bootstrap / query / tool runtime 相关 `vitest`
- `pnpm --filter agent-cli test:service-api`
- `pnpm --filter agent-cli test:mcp`
- `openspec validate "prd-21-production-architecture-and-learning" --type change`

## 当前共识

- 后续实现要更贴近你指定的 Claude Code 实际源码，不再只做“借鉴架构思想”的宽松对齐。
- 对齐顺序以外部 `src/` 为准，再结合解析文章，不以当前仓库已有写法为主。
- 学习沉淀文档只保留真正有助于学习和复盘的核心内容，不要文档元信息、参考来源、形式化废话。
- 文档命名和内容都优先中文。
- 接下来先把 agent 工具层做扎实，再做 Web 端展示。

## 下一步从哪里继续

下一轮最自然的切入点，不是继续横向加功能，而是继续对齐外部源码的模块边界：

1. 先对照外部源码中的入口、QueryEngine、query、Tool、tools、services 的真实实现方式，补一次逐文件差距分析。
2. 然后继续收口 tool contract / tool registry / tool routing，让 `base` 工具也进入更统一的执行模型。
3. 再继续收口 query engine，把 `agent-loop.ts` 往更明确的 query 核心边界推进。
4. 等 agent 运行时边界足够稳定后，再开始 Web 展示接入。

## 这次提交应包含什么

- `PRD-21` 相关 PRD / OpenSpec 变更
- `bootstrap` / `runtime` 第一轮重构代码
- 对应测试
- 中文学习沉淀文档
- 本交接文件

## 换电脑后建议先做的事

1. 拉下最新 `master`。
2. 先看 [docs/当前对话交接-2026-05-11.md](/Users/bytedance/Personal/agent-engineering-lab-web/docs/当前对话交接-2026-05-11.md:1)。
3. 再看：
   - [PRD-21-生产级架构重构与知识沉淀.md](/Users/bytedance/Personal/agent-engineering-lab-web/prd/incremental/PRD-21-生产级架构重构与知识沉淀.md:1)
   - [tasks.md](/Users/bytedance/Personal/agent-engineering-lab-web/openspec/changes/prd-21-production-architecture-and-learning/tasks.md:1)
   - [01-源码架构基线.md](/Users/bytedance/Personal/agent-engineering-lab-web/docs/learning/claude-code/01-源码架构基线.md:1)
   - [02-bootstrap与组合根第一轮.md](/Users/bytedance/Personal/agent-engineering-lab-web/docs/learning/claude-code/02-bootstrap与组合根第一轮.md:1)
   - [03-query运行时第一轮.md](/Users/bytedance/Personal/agent-engineering-lab-web/docs/learning/claude-code/03-query运行时第一轮.md:1)
   - [04-tool运行时第一轮.md](/Users/bytedance/Personal/agent-engineering-lab-web/docs/learning/claude-code/04-tool运行时第一轮.md:1)
4. 然后直接从“逐文件对齐外部 Claude Code 源码边界”开始下一轮重构。
