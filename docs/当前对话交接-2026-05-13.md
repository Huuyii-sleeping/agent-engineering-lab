# 当前对话交接

这份文档是给换电脑后继续执行时用的临时交接记录，后续可直接删除。

## 当前快照

- 分支：`master`
- 本轮遵守最新 AGENT 规则：只做本地 `commit`，不执行 `git push`
- 本地已有未推送提交：
  - `52c5a79 docs(agent): clarify push handoff rule`
- 本轮准备提交：`refactor(agent-cli): split query model boundaries`
- OpenSpec 活动 change：无，`prd-36-query-model-boundary-split` 已归档
- 归档目录：`openspec/changes/archive/2026-05-13-prd-36-query-model-boundary-split/`

## 今天刚完成的内容

本轮完成了 `PRD-36 QueryModel 模型请求模块边界收口`，并已经完成实现、验证和 OpenSpec 归档。

对应产物：
- `prd/incremental/PRD-36-QueryModel模型请求模块边界收口.md`
- `docs/learning/claude-code/23-QueryModel模型请求模块边界收口.md`
- `openspec/changes/archive/2026-05-13-prd-36-query-model-boundary-split/`

代码侧完成：
- 将 `apps/agent-cli/src/runtime/query-model.ts` 收窄为 public orchestration facade
- 新增 `apps/agent-cli/src/runtime/query-model-types.ts`
- 新增 `apps/agent-cli/src/runtime/query-model-request.ts`
- 新增 `apps/agent-cli/src/runtime/query-model-fallback.ts`
- 新增 `apps/agent-cli/src/runtime/query-model-recovery.ts`
- 新增 focused tests：
  - `apps/agent-cli/test/unit/runtime/query-model-request.test.ts`
  - `apps/agent-cli/test/unit/runtime/query-model-fallback.test.ts`
  - `apps/agent-cli/test/unit/runtime/query-model-recovery.test.ts`

本轮保持不变：
- 不改变 `requestQueryModel` public API
- 不改变 prompt envelope 或 request messages 顺序
- 不改变 `max_tokens`、tools 传递或 OpenAI request shape
- 不改变 model policy selection、budget deny、fallback once 或 usage finalize 语义
- 不改变 compact、continuation、backoff、recovery failure 或 stopReason 语义

## 已验证内容

本轮实际跑过：
- `pnpm --dir apps/agent-cli exec vitest run --no-cache test/unit/runtime/query-model.test.ts test/unit/runtime/query-model-request.test.ts test/unit/runtime/query-model-fallback.test.ts test/unit/runtime/query-model-recovery.test.ts`
- `pnpm --filter agent-cli build`
- `openspec validate --all --strict`
- `openspec list --json`
- `git diff --check`

验证结果：
- focused unit tests：4 个文件、11 个用例通过
- build：通过
- OpenSpec strict：归档前后均通过
- OpenSpec active changes：空
- diff check：通过

额外说明：
- OpenSpec 命令会出现 PostHog 网络上报失败，原因是当前环境无法访问 `edge.openspec.dev`；命令退出码为 0，不影响校验结果。
- 本轮没有需要保留的测试产物。

## 现在 PRD / docs 收口到哪里了

目前边界收口主线已经推进到：
- `PRD-30 Worktree`
- `PRD-31 TaskBoard`
- `PRD-32 Scheduler`
- `PRD-33 BackgroundTask`
- `PRD-34 Subagent`
- `PRD-35 Delivery`
- `PRD-36 QueryModel`

对应学习沉淀文档已经补到：
- `docs/learning/claude-code/17-Worktree工具模块边界收口.md`
- `docs/learning/claude-code/18-TaskBoard任务模块边界收口.md`
- `docs/learning/claude-code/19-Scheduler调度模块边界收口.md`
- `docs/learning/claude-code/20-BackgroundTask后台任务模块边界收口.md`
- `docs/learning/claude-code/21-Subagent子代理模块边界收口.md`
- `docs/learning/claude-code/22-Delivery交付验证模块边界收口.md`
- `docs/learning/claude-code/23-QueryModel模型请求模块边界收口.md`

## 下一步最自然的目标

建议下一轮优先看 `apps/agent-cli/src/runtime/query-tools.ts` 或 `apps/agent-cli/src/runtime/query-finalization.ts`。

原因：
- QueryModel 收口后，模型请求、fallback 和 recovery 的内部边界已经清楚。
- Query runtime 剩余风险更可能在 tool stage 或 finalization stage：工具结果回填、hook stop、auto delivery、todo/reset 这类流程仍可能聚合在单个阶段文件里。

备选目标：
- 继续检查 `apps/agent-cli/src/runtime/` 下是否还有单文件聚合了策略、执行和状态变更。
- 如果要扩展 QueryModel 能力，单独开新 PRD 处理多角色 model routing、请求参数策略或 recovery budget，不要混进边界收口。

## 换电脑后怎么继续

1. 在新电脑拉最新 `master` 后，注意当前本地有未推送提交需要由用户手动 push。
2. 先看这份交接文档。
3. 再看：
   - `prd/incremental/PRD-36-QueryModel模型请求模块边界收口.md`
   - `docs/learning/claude-code/23-QueryModel模型请求模块边界收口.md`
   - `openspec/changes/archive/2026-05-13-prd-36-query-model-boundary-split/`
4. 确认工作树干净后，直接开下一轮 PRD / OpenSpec change。

## 这次切换机器前不需要再做的事

- 不需要补做 PRD-36 验证：已跑完。
- 不需要收尾活动 change：当前没有活动 change。
- 不需要清理测试产物：本轮没有残留。
- 不需要我执行 `git push`：按 AGENT 规则，push 由用户手动执行。
