# from-scratch-agent

一个在 `agent_dev` 下从零开始实现的独立 Agent 项目。

## 约束

- 不依赖 `D:/项目/agent/typescript` 目录中已有的示例代码。
- 按需求逐步实现功能，每一步都可单独讲解与验证。

## 开发

1. 安装依赖：`npm install`
2. 本地运行：`npm run dev`
3. 构建：`npm run build`
4. 运行构建产物：`npm run start`
